spring.application.name=springboot-basic-advance-crud-practice

# === Server Port (optional) ===
server.port=5000

# === Datasource Configuration ===
spring.datasource.url=jdbc:postgresql://localhost:5432/prac
spring.datasource.username=postgres
spring.datasource.password=Piyush@l1
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA (Hibernate) Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === Logging (optional) ===
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# Maximum size of a single uploaded file
spring.servlet.multipart.max-file-size=100MB

# Maximum size of the total request (all files + data)
spring.servlet.multipart.max-request-size=100MB